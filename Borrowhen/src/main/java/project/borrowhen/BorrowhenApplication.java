package project.borrowhen;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class BorrowhenApplication {

	public static void main(String[] args) {
		SpringApplication.run(BorrowhenApplication.class, args);
	}
	
	/*
	 * 
	 * This is a rush project so it has spaghetti codes or redundant codes.
	 * 
	 * 
	 */

}
